---
name: Bug report
about: Report a defect or unexpected behavior in loractl
title: ""
labels: bug
assignees: laurigates
---

## What happened

A clear description of the bug.

## Expected behavior

What you expected instead.

## Reproduction

Steps to reproduce — ideally the exact command and config:

```
just train config/examples/lora.yaml
```

- Config / env / CLI flags used:
- Feature flags (`mnist` / `gpt2-real` / `wgpu`), if any:

## Environment

- loractl version / commit:
- OS + arch (e.g. macOS arm64):
- Rust toolchain (`rustc --version`):

## Logs / output

Paste the relevant error output (set `RUST_LOG=debug` for more detail).
